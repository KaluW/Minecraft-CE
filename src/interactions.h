#ifndef INTERACTIONS_H
#define INTERACTIONS_H

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>
#include <tice.h>

// List of interactions. Chose not to have an 'updateInteractions()' function,
// as an individual interaction function should only need to be called when it is triggered.
// Looping through possible interactions every iteration of the game engine seems like a waste of time.

#endif